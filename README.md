# Mac Cleaner
Desktop app for finding removable clutter on macOS without risky automation.

## Run
```bash
npm install
npm run dev
```
Open `http://localhost:3000`.

## Test
There is no automated test suite yet. For now, validate the app with:

```bash
npm run lint
npm run build
```
